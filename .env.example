# Cloudflare API（本地 purge cache 用，CI 用 GitHub Secrets 同名变量）
# ---
# 获取方式：
# 1. CF_API_TOKEN: dash.cloudflare.com/profile/api-tokens → Create Custom Token
#    Permissions: Zone | Cache Purge | Purge
#    Zone Resources: Include | Specific zone | laodao-ai.com
# 2. CF_ZONE_ID_COM: dash.cloudflare.com → 选 laodao-ai.com → 右下 API → Zone ID
#
# 复制本文件为 .env（已 gitignore），填入实际值后 scripts/purge-cf-cache.sh 自动加载
CF_API_TOKEN=
CF_ZONE_ID_COM=
